import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'How KPI·FIT works',
  description:
    'How KPI·FIT works: import workout logs (Hevy/Strong/Lyfta/CSV), compute analytics locally in your browser, and explore dashboards for volume, PRs, and muscle emphasis.',
} satisfies Config;
